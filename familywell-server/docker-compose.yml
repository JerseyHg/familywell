version: "3.8"

services:
  api:
    build: .
    container_name: familywell-api
    restart: always
    ports:
      - "127.0.0.1:8004:8000"
    env_file:
      - .env
    volumes:
      - ./app:/app/app
    networks:
      - shared-net

networks:
  shared-net:
    external: true